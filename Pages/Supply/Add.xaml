<Page x:Class="VinylRecordsApplication.Pages.Supply.Add"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VinylRecordsApplication.Pages.Supply"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Add">

    <Grid Background="#FFE6E6E6">
        <Border Background="#FFFAFAFA" BorderBrush="#FFFAFAFA" Margin="20" BorderThickness="1" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="Black" Direction="315" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>
            <Grid Margin="15">
                <Image Width="68" Height="68" VerticalAlignment="Top" HorizontalAlignment="Left"
                       Source="/Image/ic_supply.png"/>
                <Grid Margin="73,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="#Наименование поставщика:" Margin="0,0,10.2,0" VerticalAlignment="Top"/>

                    <ComboBox x:Name="tbManufacturer" Margin="0,26,10,0" Height="25"
         VerticalContentAlignment="Center" VerticalAlignment="Top" Padding="10,0">
                        <ComboBox.Effect>
                            <DropShadowEffect BlurRadius="10" Color="Black" Direction="315" ShadowDepth="2" Opacity="0.5"/>
                        </ComboBox.Effect>
                    </ComboBox>

                    <Label Content="#Даты поставки:" Margin="0,53,10,0" VerticalAlignment="Top" Grid.ColumnSpan="2"/>

                    <DatePicker x:Name="tbDateDelivery" Height="25" Margin="0,79,10.2,0" VerticalAlignment="Top">
                        <DatePicker.Effect>
                            <DropShadowEffect BlurRadius="10" Color="Black" Direction="315" ShadowDepth="2" Opacity="0.5"/>
                        </DatePicker.Effect>
                    </DatePicker>

                    <Label Content="#Наименование пластинки:" Margin="10,0,9.8,0" VerticalAlignment="Top" Grid.Column="1"/>

                    <ComboBox x:Name="tbRecord" Margin="10,26,10,0" Height="25" Grid.Column="1"
         VerticalContentAlignment="Center" VerticalAlignment="Top" Padding="10,0">
                        <ComboBox.Effect>
                            <DropShadowEffect BlurRadius="10" Color="Black" Direction="315" ShadowDepth="2" Opacity="0.5"/>
                        </ComboBox.Effect>
                    </ComboBox>

                    <Label Content="#Количество поставки:" Margin="10,53,10,0" VerticalAlignment="Top" Grid.Column="1"/>

                    <TextBox x:Name="tbCount" Height="25" Margin="10,79,10.2,0" TextWrapping="Wrap"
         VerticalContentAlignment="Center" Padding="10,10" VerticalAlignment="Top" Grid.Column="1" PreviewTextInput="tbPreviewNumber">
                        <TextBox.Effect>
                            <DropShadowEffect BlurRadius="10" Color="Black" Direction="315" ShadowDepth="2" Opacity="0.5"/>
                        </TextBox.Effect>
                    </TextBox>

                    <Button x:Name="addBth" Content="Добавить" HorizontalAlignment="Right" Margin="0,0,10,10" VerticalAlignment="Bottom"
                            Width="100" Grid.Column="2" Height="25" BorderBrush="#FFFF9800" Background="#FFFF9800" Foreground="White"
                            FontWeight="Bold" Click="AddSupply">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" Color="Black" Direction="315" ShadowDepth="2" Opacity="0.5"/>
                        </Button.Effect>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
